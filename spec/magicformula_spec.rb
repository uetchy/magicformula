require 'spec_helper'

describe Magicformula do
  it 'has a version number' do
    expect(Magicformula::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
